import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Services — Sourcing, Distribution & Technical Solutions",
  description: "Spot market sourcing, yearly distribution programs, combined shipping, PCB design, counterfeit detection, and technical consulting from Costa Devices.",
};

export default function ServicesLayout({ children }: { children: React.ReactNode }) {
  return children;
}



