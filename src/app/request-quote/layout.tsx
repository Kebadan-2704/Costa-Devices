import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Request a Quote — Get Pricing in 24 Hours",
  description: "Submit a quote request for EV fuses, circuit protection, capacitors, and more. Our team responds with competitive pricing within 24 hours.",
};

export default function RequestQuoteLayout({ children }: { children: React.ReactNode }) {
  return children;
}



